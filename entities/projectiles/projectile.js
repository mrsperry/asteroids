class projectile extends entity {
    constructor(faction, start, target) {
        super(faction, start);

        this.target = target;
    }

    update() {
        super.update();
    }

    draw(r, g, b) {
        super.draw(r, g, b);
    }
}