class projectile extends entity {
    constructor(faction, start, target) {
        super(faction, start);

        this.target = target;
    }

    update() {
        super.update();
    }

    draw(color) {
        super.draw(color);
    }
}